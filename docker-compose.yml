services:
  clanker:
    build: .
    image: clanker:dev
    env_file:
      - .env
    environment:
      CLANKER_TRANSPORTS: discord
    volumes:
      - ./sessions:/app/sessions
    restart: unless-stopped
